/*
Si fornisca la definizione delle strutture dati LIST e NODE, come ADT di I categoria e quasi ADT
rispettivamente, per rappresentare una lista singolo linkata di interi, senza sentinelle. Suddividere il codice in
modo opportuno tra file .h e .c Si scriva una funzione void f(LIST l) che ricevuta in input una lista di interi 
(rappresentata facendo riferimento ai tipi definiti in precedenza), già ordinata in ordine crescente, 
compatti i contenuti della lista cancellando tutti i nodi uguali consecutivi, ad eccezione del primo di ogni gruppo.*/

#include <stdio.h>

int main(void){
    
    return 0;
}
